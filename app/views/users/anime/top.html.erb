<h1>新着・ランキング</h1>

<div class="box">
	<div class="box1">
		<h1>タグ</h1>
		<% @tags.each do |tag| %>
		<%= link_to "#{tag.name}(#{tag.taggings_count})" %>
		<% end %>
	</div>

	<%= render 'list' %>
</div>